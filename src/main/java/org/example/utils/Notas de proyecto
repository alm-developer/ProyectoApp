

1. Poner las listas en privado
2. Cambiar las cosas innecesarias quitando el estatico de algunas ( statico solo exsiste en esa clase)

  Este fragmento obtiene la lista de almacenes,
  obtiene el tamaño y luego le resta uno para asignarle el primer objeto de la lista,
  que es basicamente el almacen que acabamos de crear cuando la lista esta en cero,
  y luego hemos obtenido el id y lo hemos seleccionado,
  creamos una "variable" tipo almacen para guardar el objeto

  Almacen recienCreado = GestionAlmacen.getListaAlmacenes().get(GestionAlmacen.getListaAlmacenes().size() - 1);
  seleccionarAlmacen(recienCreado.getIdAlmacen());

